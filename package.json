{
  "name": "prompt-copilot",
  "version": "1.0.0",
  "type": "module",
  "description": "AI-assisted prompt review application",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.1",
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^6.4.3",
    "wrangler": "^4.103.0"
  }
}
